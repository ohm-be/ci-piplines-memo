pipeline {
    agent any
    parameters {
        string(name: 'TAG', defaultValue: '', description: 'Tag to deploy')
    }
    stages {
        stage('Deploy to SIT') {
            steps {
                echo "Deploying SIT with TAG: ${params.TAG}"
                // deploy SIT logic
            }
        }
    }
}
